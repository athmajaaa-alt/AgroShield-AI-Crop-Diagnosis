
A simple AI project to detect plant diseases from text observation and give advice.

## How to Run

1. Install dependencies: `pip install -r requirements.txt`  
2. Run app: `streamlit run app/streamlit_app.py`  
3. Enter plant name and observed symptoms → get disease info and solution.